#include <stdio.h>

int main() {
    int n;
    int count = 1;
    scanf("%d", &n);
        for(int i = 0; n > i; i ++) {
            for(int x = 0;x < n; x++) {
                printf("%d", count);
                count++;
            
        }
        printf("\n");

    
    }
    return 0;
}
